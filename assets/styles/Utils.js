import { StyleSheet } from "react-native";

export const utils = StyleSheet.create({
  // Utils

  // Alignment
  jcCenter: {
    justifyContent: 'center',
  },

  // Margin
  mt0: {
    marginTop: 0
  },
  mb5: {
    marginBottom: 5 
  },
  mb15: {
    marginBottom: 15,
  }
});
